(import ["@daklang/transpiler" [transpile-str]])

(const ext ".dak")

(fn ^:export dakPlugin []
  {:name :dak-lang
   :transform
   (fn [src id]
     (when (id.endsWith ext)
       (transpile-str src {:filename id})))})
