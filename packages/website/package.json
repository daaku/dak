{
  "name": "@daklang/website",
  "version": "0.1.0",
  "description": "Website package for Dak",
  "homepage": "https://daklang.com/",
  "license": "MIT",
  "author": "Naitik Shah <n@daaku.org>",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/daaku/dak",
    "directory": "packages/website"
  },
  "bugs": {
    "url": "https://github.com/daaku/dak/issues",
    "email": "bugs@daklang.com"
  },
  "scripts": {
    "dev": "NODE_ENV=development NODE_OPTIONS='--no-warnings --enable-source-maps --loader @daklang/loader' node build.dak",
    "build": "NODE_ENV=production NODE_OPTIONS='--no-warnings --enable-source-maps --loader @daklang/loader' node build.dak",
    "watch": "watchexec -cr -- npm run --silent dev"
  },
  "dependencies": {
    "@daklang/transpiler": "^0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.6",
    "finalhandler": "^1.2.0",
    "serve-static": "^1.15.0",
    "ws": "^8.12.0"
  }
}
